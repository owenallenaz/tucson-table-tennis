# tucson-table-tennis

TODO
Add edit button to match rows
Update getMatches to return matches that don't have a result
Add rating explanation section
Add final rating to the player row
Add tournament result section to home
Separate roster tab and matches tab so that a singular tab can represent a singular tournament
Allow loading a different tab for the various match settings, default to using the current tab if not specified
